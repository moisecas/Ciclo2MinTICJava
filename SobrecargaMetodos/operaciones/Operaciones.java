package SobrecargaMetodos.operaciones;

public class Operaciones {
    public static int sumar (int a, int b) {
        return a + b; //retorna la suma de los dos argumentos
    }

    public static double sumar (double a, double b) { //sobrecarga de métodos con argumentos de tipo double
        return a + b; //retorna la suma de los dos argumentos
    }
        
    
}
